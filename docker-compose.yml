version: '3.8'

services:
  latex-compiler:
    build: .
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
    volumes:
      - ./temp:/app/temp
      - ./output:/app/output
    restart: unless-stopped
