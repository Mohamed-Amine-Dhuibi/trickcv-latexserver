version: '3.8'

services:
  latex-compiler:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
    volumes:
      - ./temp:/app/temp
      - ./output:/app/output
    restart: unless-stopped
